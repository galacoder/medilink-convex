/**
 * packages/db/src/schema.ts: Convex schema reference stub.
 *
 * The canonical schema is defined in `convex/schema.ts` at the monorepo root.
 * Tables defined there:
 *   - organizations     (org_type: "hospital" | "provider")
 *   - organizationMemberships (role: "owner" | "admin" | "member")
 *   - users             (optional platformRole: "platform_admin" | "platform_support")
 *
 * See convex/schema.ts for full table definitions, indexes, and field types.
 * See packages/validators/src/organizations.ts for Zod input validation.
 */
export {};
